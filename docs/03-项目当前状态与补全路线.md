# 项目当前状态与补全路线

## 1. 当前结论

这个仓库目前不是完整货运平台，而是一个 React Native 货主端本地原型。它已经把默认模板替换成了货主端入口，并做了登录/注册、本地首页、发单草稿、订单列表、订单详情的静态或半交互页面。

当前主要问题不是页面完全没有，而是业务闭环还停留在本地演示层：数据仍是本地 mock，没有后端、地图、真实支付、推送和真实认证。货主端已经能完成本地首启引导、引导完成状态持久化、网络异常页本地演练、本地 API 重试队列、失败标记和重新检测清空后返回、本地登录、发单、议价订单本地发布、一口价本地范围校验、货物重量 0.1 到 50 吨本地范围校验、货物体积/数量有效性校验、货物描述 200 字上限校验、装货时间明确性校验、装货时间 2 小时后/7 天内/半小时刻度本地校验、期望送达时间/尽快送达本地记录、装卸地址相同拦截、装货/卸货备注 50 字本地记录、读取个人中心本地常用地址建议并回填装卸地址/联系人/电话、读取个人中心本地常用联系人建议并回填装卸联系人/电话、车辆车型/车长/尾板/篷布要求本地记录、装卸/保价/包装增值服务本地记录、装卸工人数 1-5 人本地选择、保价货值本地校验和展示、货物图片凭证 1-6 张本地计数占位、本地凭证清单和移除最新凭证、草稿本地持久化适配层恢复、草稿版本/保存时间记录、24 小时有效期、无效/过期快照清理、草稿同步状态边界和本地重试、草稿同步失败队列本地标记与重试、登录/注册验证码本地固定码匹配、错误验证码拦截、60 秒重新获取冷却、5 分钟有效期和同手机号 1 小时 5 次发送上限校验、支付方式本地选择、固定价订单本地可用优惠券计价预览、优惠金额/实付金额记录、订单详情展示、发布后本地标记优惠券已使用、带券订单消费记录展示原价/优惠券/优惠金额/实付金额、列表查看、订单状态/时间筛选、自定义日期范围本地筛选、详情查看、订单同步失败队列本地标记与重试、详情按来源返回订单列表或消息中心、待接单修改订单、非待接单修改申请费用/退款/司机通知提示和本地确认/驳回、状态流转、取消订单并记录原因、本地违约提示、本地结算结果、本地退款状态、本地客服审核和司机通知、查看司机报价并选择司机、订单联系人/已接单司机/客服热线系统拨号、待接单追加赏金、议价订单等待报价空状态、查看运输中订单本地位置跟踪、确认送达、异常上报、异常说明最小长度校验、异常图片凭证占位、异常处理状态流转、评价司机、评价内容 6-200 字校验、匿名评价、评价图片凭证占位、评价记录同步到个人中心、已评价订单查看评价且不可再次编辑、我的评价本地筛选和司机回复展示、常用路线复用和本地新增/编辑/删除/排序、常用路线同步状态边界和本地重试、重新下单并记录复制来源、历史订单缺字段时补齐可发布的数量和装货时间默认值、城市本地切换、消息中心查看、订单消息跳转详情、订单/系统消息本地已读和首页未读数联动、客服帮助查看、客服热线拨号、投诉建议本地工单提交、本地持久化恢复和本地处理记录流转、个人中心查看、个人中心顶部头像占位/脱敏手机号/实名与企业状态展示、个人中心下钻页查看、个人中心资料同步失败队列本地标记与重试、常用地址本地新增编辑和删除前确认、联系人电话校验和 20 个上限拦截、常用联系人本地新增编辑删除、手机号格式校验和 50 个上限拦截、实名认证本地提交、审核中、本地驳回原因展示和重新提交、企业认证本地提交、审核中、本地驳回原因展示和重新提交、消费记录本地查看、完成/进行中/退款筛选、时间筛选、总消费统计和支付/托管/退款明细展示、发票可开票订单多选、金额汇总、类型/抬头/接收邮箱本地申请记录、企业发票抬头读取本地企业认证名称、增值税专用发票企业认证前置拦截、发票本地审核通过/下载凭证/驳回原因/重新提交状态流转、优惠券本地查看/筛选/使用状态、设置页昵称/头像/手机号换绑/登录密码本地更新、通知开关、异地登录保护本地开关、本机设备检查占位、协议摘要/隐私政策本地确认/权限用途说明查看、权限状态本地检查和拒绝引导、版本更新本地检查和退出登录等 MVP 主链路，但真实支付、司机端、后台和真实 API 仍未开发。

本轮继续补齐带券订单进入个人中心消费记录后的本地明细展示：固定价订单选择本地可用优惠券并发布后，消费记录会展示原价、优惠券、优惠金额和实付金额；这仍是本地订单运行态字段展示，真实优惠券核销、锁券、后端计价、支付抵扣、真实支付流水和跨设备优惠券同步仍未接入。

本轮完整平台规划已进入阶段 0/1 实施准备：新增完整平台总体架构规格和阶段 0/1 实施计划，默认采用 NestJS + PostgreSQL + Redis + S3 兼容对象存储 + 高德地图 + Web 后台路线；同时已新增 `apps/api` NestJS 后端骨架、阶段 1 Prisma schema、认证基础服务、移动端平台认证 API adapter 和平台工程文档。下一步继续把本地认证页接入真实 API adapter，并把验证码、token、refresh token 和用户资料从内存演示推进到数据库持久化；司机端、后台、地图、支付、推送、上传和 IM 仍保持后续独立阶段，不在阶段 0/1 混开。

## 2. 已经开发的内容

### 2.1 工程基础

- React Native 0.86 + React 19 + TypeScript。
- Android / iOS 原生工程目录已存在。
- `package-lock.json` 已生成，依赖已经安装。
- `@react-native/new-app-screen` 已从依赖中移除。
- `react-native-safe-area-context` 已接入。
- Jest、ESLint、TypeScript 配置可用。

### 2.2 应用入口与代码结构

- `App.tsx` 已不再展示 React Native 默认模板。
- 当前无有效会话且未完成引导时先进入本地首启引导；已完成引导后进入本地认证页；有有效会话时直接恢复到货主端首页。
- 认证成功后进入货主端首页。
- 认证成功后会写入基于 AsyncStorage 的本地认证会话适配层，App 重建后可在会话有效期内恢复到首页；本地会话支持过期和刷新，退出登录会清理本地会话。
- 顶层 `App.tsx` 只保留本地导航状态和订单状态协调，包含：
  - `auth`
  - `onboarding`
  - `home`
  - `network-error`
  - `order-draft`
  - `orders`
  - `order-detail`
- 订单列表支持从首页“全部订单”和状态卡带筛选进入。
- 发单页支持未完成草稿本地存储适配层恢复，也支持从常用路线、历史订单或待接单订单预填。
- 已拆出 `src/` 目录：
  - `src/types.ts`
  - `src/data/mockData.ts`
  - `src/utils/order.ts`
  - `src/styles.ts`
  - `src/components/*`
  - `src/screens/*`
  - `src/screens/order-draft/*`
  - `src/screens/order-detail/*`
  - `src/screens/profile/*`
- 个人中心已从 `HomeScreen.tsx` 拆到 `src/screens/ProfileCenterScreen.tsx`，避免首页继续膨胀。
- 发单页里的货物、装卸地址、车辆与时间、增值服务、价格和支付方式区块已拆到 `src/screens/order-draft/`，主页面继续保留表单状态、校验和发布流程。
- 订单详情里的司机信息卡、司机报价卡、位置跟踪、追加赏金、异常上报、取消订单、修改申请和评价司机表单已拆到 `src/screens/order-detail/`，避免继续把详情页堆成一坨。
- 个人中心里的常用地址、常用联系人、我的评价、消费记录、发票管理、优惠券、设置记录、实名认证和企业认证表单已拆到 `src/screens/profile/`，父页面保留概览、入口协调和本地状态持久化。

### 2.3 认证入口

- 已有登录/注册切换。
- 登录支持手机号 + 获取验证码状态 + 6 位验证码格式和本地固定码匹配校验，错误验证码会拦截。
- 注册支持手机号、获取验证码状态、验证码格式和本地固定码匹配、密码强度和协议勾选本地校验；密码需至少 6 位并包含字母和数字。
- 获取验证码只做本地提示和本次运行内发送状态记录，已补本地演示固定验证码、错误码拦截、60 秒重新获取冷却、5 分钟有效期和同手机号 1 小时 5 次发送上限校验，没有真实短信接口。
- 已有本地认证会话保存、App 重建恢复、会话过期、会话刷新和退出清理。
- 已覆盖登录进入首页、注册协议、注册密码强度、未获取验证码禁止登录/注册、错误验证码拦截、验证码 60 秒重新获取冷却、同手机号 1 小时 5 次发送上限、验证码过期、会话恢复、会话过期和会话刷新测试。

### 2.4 货主首页

首页已包含 `02-货主端MVP首页页面规格.md` 中的主要静态区域：

- 城市、消息、客服、退出入口。
- 用户认证状态卡。
- 快速发单入口。
- 订单状态概览。
- 常用路线。
- 最近订单列表。

### 2.5 订单相关页面

- 已有本地发单页，覆盖货物、货物图片凭证 1-6 张本地计数占位、本地凭证清单、移除最新凭证、地址、个人中心本地常用地址建议回填、个人中心本地常用联系人建议回填、车辆、装货时间、期望送达时间、价格、本地可用优惠券计价预览和确认发布预览。
- 最终确认发布后会生成本地订单，并跳转到订单详情。
- 本地新发布订单、待接单订单修改和详情内状态变更会记录后端同步边界，订单详情展示“待同步/已同步/同步失败”状态和本地同步队列，并支持本地标记同步失败、重试标记为已同步并清空队列；这仍是本地边界，不代表真实 API 已接通。
- 未完成草稿离开发单页后可自动保存，并可通过本地存储适配层在 App 重建后恢复；超过 24 小时的草稿会本地清理，不再回填；草稿变更会展示“待同步/已同步/同步失败”边界，并支持本地重试标记为已同步。
- 已有订单列表页，支持本地 tab 过滤：
  - 全部订单
  - 待接单
  - 进行中
  - 待确认
  - 已完成
  - 已取消
- 订单列表支持按订单号、地址、货物、车辆要求、司机姓名进行本地搜索。
- 订单列表支持按全部时间、今天、本周、历史和自定义日期范围进行本地时间筛选。
- 已有订单详情页，展示路线、状态时间线、货物、车辆要求、价格、本地优惠券优惠金额/实付金额和主/次操作按钮。
- 订单详情补充展示装卸联系人、电话、装货/卸货备注、装货时间、期望送达时间、货物数量、货物描述、增值服务、支付方式和司机信息。
- 订单详情支持通过系统拨号联系装货联系人和卸货联系人。
- 待接单详情支持本地查看司机报价列表，并选择司机进入待装货。
- 已接单详情支持通过系统拨号联系司机，并保留本地联系提示。
- 待接单详情支持本地追加赏金，并在订单详情记录曝光赏金。
- 议价订单可不填写一口价发布，详情展示“司机报价”，展开报价列表时展示等待司机提交报价的空状态。
- 订单详情支持按进入来源返回订单列表或消息中心，避免从列表/消息查看详情后被固定带回首页。
- 待装货、运输中、待确认订单支持提交本地修改申请，并展示待客服确认状态、司机通知、费用影响、退款状态和本地确认/驳回结果。
- 订单详情支持本地状态推进：
  - 待接单 → 待装货
  - 待装货 → 运输中
  - 运输中 → 待确认
  - 待确认 → 已完成
- 待确认订单的主按钮支持本地确认送达并进入已完成。
- 待接单、待装货订单支持填写取消原因后本地取消，并展示本地违约提示、结算结果、退款状态、客服审核和司机通知。
- 运输中、待确认订单支持本地异常上报和图片凭证占位；异常上报要求显式选择异常类型，并填写至少 6 个字的异常说明，提交后可把处理状态从待客服跟进更新为已处理。
- 已完成订单支持本地评价司机。
- 评价司机要求选择标签并填写 6-200 字的评价内容，支持本地匿名开关。
- 评价司机支持本地图片凭证占位。
- 已提交评价会随本地订单状态持久化，并同步到个人中心“我的评价”记录，已评价订单主操作切换为“查看评价”并阻止再次编辑。
- 我的评价支持按全部、5 星、4 星及以下进行本地筛选。
- 我的评价支持展示本地 mock 司机回复和回复时间。
- 已完成、已取消订单支持重新下单并预填发单表单；历史订单缺货物数量或装货时间不可发布时，会补齐可发布的默认数量和装货时间；新发布的本地订单会记录复制来源订单号、来源说明和复制时间。
- 待接单订单支持进入发单页修改，并覆盖原订单。
- 常用路线可预填发单表单。
- 首页最近订单可进入订单详情。
- 最近订单标题可进入订单列表。
- 首页状态卡可进入订单列表并带对应筛选。
- 首页订单状态标题右侧“全部订单”可进入订单列表。
- 首页消息入口已接入本地消息中心，展示订单消息、系统通知和客服进度；订单类消息可跳转到对应订单详情，订单和非订单消息都可本地持久化已读状态、联动首页未读数。
- 首页客服入口已接入本地客服帮助，展示常见问题和服务渠道，支持客服热线系统拨号，并支持投诉建议本地工单提交、受理/处理完成状态流转、处理记录展示和 AsyncStorage 冷启动恢复。
- 首页个人中心入口已接入本地个人中心，展示头像占位、昵称、脱敏手机号、实名/企业认证状态、常用功能入口和本地下钻页。
- 个人中心下钻页已支持本地查看常用地址、常用联系人、我的评价、消费记录、实名认证、企业认证、发票管理、优惠券和设置。
- 个人中心资料变更已支持可见的后端同步边界，账号资料、认证、地址、联系人、发票、优惠券和设置等本地变更会标记“待同步”，并展示本地同步队列；总览页支持把队列本地标记为“同步失败”，也支持本地重试标记为“已同步”并清空待同步队列。这仍是本地边界，不代表真实账号中心 API 已接通。
- 常用地址支持本地新增、编辑和删除前确认，校验联系人文本中的 11 位手机号，并在新增时拦截超过 20 个的本地上限。
- 常用联系人支持本地新增、编辑和删除，校验 11 位手机号，并在新增时拦截超过 50 个的本地上限；发单页可读取个人中心当前本地常用联系人建议并回填装卸联系人和电话。
- 实名认证支持填写真实姓名、身份证号、身份证正反面凭证占位和人脸核验占位，提交后展示本地审核中状态，并支持本地模拟驳回原因展示和重新提交。
- 企业认证支持填写企业名称、统一社会信用代码、法人姓名、法人身份证号、企业联系电话和营业执照凭证占位，提交后展示本地审核中状态，并支持本地模拟驳回原因展示和重新提交。
- 消费记录支持按全部、已完成、进行中、退款进行本地筛选，也支持近期/历史时间筛选、总消费统计，并展示支付时间、支付状态、托管金额、司机收入、退款进度，以及带券本地订单的原价、优惠券、优惠金额和实付金额。
- 发票管理支持选择可开票订单并汇总金额，选择发票类型、发票抬头并填写接收邮箱；企业抬头会优先读取本地企业认证名称；增值税专用发票会先校验本地企业认证资料已提交且未驳回；本地提交后记录申请明细，且支持审核通过、下载凭证、驳回原因展示和驳回后重新提交，把状态从待提交推进到申请中、已开票或已驳回。
- 优惠券支持按全部、可使用、已使用、已过期进行本地筛选，并可把可使用券标记为已使用；发单固定价订单可选择本地可用券并展示本地优惠金额和实付金额，发布后会在个人中心本地标记该券已使用并记录订单号，带券订单进入消费记录后会展示原价、优惠券、优惠金额和实付金额。
- 设置页支持昵称修改、头像凭证占位、手机号换绑本地校验、登录密码本地修改、手机号保护/异地登录保护/订单通知/促销通知本地开关、本机设备检查占位、用户协议/隐私政策摘要与本地确认/权限说明/权限状态本地检查和拒绝引导/版本更新/关于我们摘要查看、退出登录，并展示本地更新提示。

### 2.6 自动化检查

当前已验证：

- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm test`：176 个测试通过。

注意：npm 当前会输出本机配置 warning，例如 `Unknown user config "home"`，但没有阻塞测试、类型检查或 lint。

## 3. 已开发一半的内容

### 3.1 发单流程

当前已打通本地发单闭环：

- 货物类型可选。
- 货物重量、体积、数量、描述可填写，货物描述最多 200 字。
- 货物重量必须包含有效数字，并满足 0.1 到 50 吨本地发布范围；支持 kg/公斤/千克文本按吨换算。
- 货物体积如填写需在 0.1 到 100 立方米之间。
- 货物数量必须包含有效正数，纯文本会拦截发布。
- 装货/卸货地址、50 字备注、联系人、电话可填写，并支持从个人中心当前本地常用地址建议一键回填地址、联系人和手机号，也支持从个人中心当前本地常用联系人建议一键回填装卸联系人和手机号。
- 装货地址和卸货地址相同时会拦截发布。
- 车型、车长、尾板和篷布要求可选择，装货时间需包含明确日期/相对日期和时间，并满足当前时间 2 小时后、7 天内、半小时刻度的本地发布规则。
- 期望送达时间支持填写明确时间或选择“尽快送达”，会进入确认页、订单详情和本地持久化订单状态；乱填文本会拦截发布。
- 支持装卸协助、保价运输、防震包装等增值服务本地选择；装卸协助可选择 1-5 人，保价运输需填写有效货值，并在确认页和订单详情展示。
- 支持一口价和议价模式；一口价需在 1 到 50000 元之间，议价订单可不填一口价发布，并在详情报价列表展示等待报价空状态。
- 支持货到付款和在线支付的本地选择，确认页和订单详情会展示支付方式；在线支付只提示本地不扣款，真实微信/支付宝待接入。
- 固定价订单可选择个人中心当前本地可用优惠券，按本地“满 X 减 Y”文案计算预览优惠金额和实付金额，并在订单详情和消费记录里展示原价、优惠券、优惠金额和实付金额；发布后会把该券在个人中心本地标记为已使用并记录订单号；真实券核销、锁券、后端计价和支付抵扣仍未接入。
- 支持本地货物图片凭证 1-6 张计数占位、本地凭证清单和移除最新凭证，并在订单详情展示数量；超过 6 张会提示本地上限。
- 预览确认后会生成本地订单。
- 最终确认发布后跳转到订单详情。
- 未完成草稿会写入本地草稿存储适配层，App 重建后再次发单可恢复；适配层会记录版本和保存时间，并在读取到无效或超过 24 小时的快照时自动清理。
- 待接单订单可回填到发单页修改，预填装货时间如已不符合本地发布时间规则会补齐可发布默认值，发布后覆盖原订单。

缺口：

- 草稿已从 App 内存状态推进到带版本、保存时间、24 小时有效期和同步状态边界的 AsyncStorage 本地存储适配层，并覆盖 App 重建后的恢复、无效快照清理、过期清理、本地同步队列、同步失败标记和本地重试清空；仍缺少跨设备草稿同步、服务端冲突处理和后端草稿接口。
- 地址已从纯文本输入补到读取个人中心本地常用地址建议并回填，联系人也已补到读取个人中心本地常用联系人建议并回填；但没有真实地图选点、地址搜索服务、经纬度解析、后端地址标准化、真实通讯录、账号中心联系人同步或跨设备同步。
- 车辆要求仍是本地记录，没有真实车型匹配、运力约束校验或司机端接单偏好联动。
- 没有真实货物照片选择、真实缩略预览、上传或后端存储；当前清单只是本地占位。
- 本地优惠券计价只解析当前 mock 优惠券文案并展示预览、详情和消费记录明细，发布后只更新设备内本地使用状态；不会真实核销优惠券、锁券、支付抵扣或调用后端计价。
- 没有真实后端创建订单接口。
- 待接单订单支持直接修改；待装货、运输中、待确认订单支持本地修改申请、费用影响、退款状态、司机通知和本地确认/驳回记录，但真实司机端同步、费用重算和退款联动仍未接入。

### 3.2 订单管理

当前列表和详情能展示本地订单，并支持本地状态筛选、时间筛选、自定义日期范围筛选、搜索、司机报价选择、待接单追加赏金、状态流转、非待接单修改申请和带原因取消。

缺口：

- 订单详情的装货联系人、卸货联系人、已接单司机和客服帮助里的客服热线可拉起系统拨号；即时聊天仍未接入。
- 查看位置已支持本地位置跟踪卡，展示司机、路线、当前位置描述和预计到达；真实地图、定位刷新和路线轨迹仍未接入。
- 待接单订单修改已支持本地覆盖原订单；其他状态修改已支持本地申请、费用/退款/司机通知提示和确认/驳回流转，但不会真实改写已接单订单字段。
- 订单本地新增、编辑和详情状态变更已支持可见的后端同步状态边界、本地同步队列、失败标记和本地重试清空；没有真实 API 请求、网络失败捕获、队列重放或服务端冲突处理。
- 异常上报支持本地表单、必选类型、说明最小长度校验、图片凭证占位和本地处理状态流转，但没有真实照片上传、异常链路里的真实客服工单或真实客服处理记录。
- 重新下单已支持预填表单，能在历史订单缺货物数量或装货时间不可发布时补齐本地默认值，并在新发布的本地订单详情和持久化订单状态中记录复制来源；真实后端复制订单审计记录仍未接入。
- 时间筛选仍是基于本地中文时间文案的演示逻辑，自定义日期范围也只解析本地文案；没有真实日期字段、真实日期范围 API 或后端筛选。
- 评价司机已支持本地表单、标签必选、评价内容 6-200 字校验、匿名开关、图片凭证占位、通过本地订单状态持久化同步到个人中心评价记录、已提交评价不可再次编辑、本地评分筛选和本地 mock 司机回复展示，但没有真实图片上传、更完整标签规则、后端评价同步或真实司机回复同步。
- 取消订单已有本地原因记录、违约提示、结算结果、退款状态、客服审核、司机通知和消费记录里的本地退款中展示，但没有真实后端违约金计算、真实退款进度、客服审核流转或司机端同步。

### 3.3 首页入口

首页结构已经比较完整，但仍有少量入口是占位：

- 城市选择已支持本地切换和提示，但没有定位权限、城市服务、跨城计价或后端同步。
- 网络状态入口已支持本地网络异常演练页，展示本地缓存可用范围、本地 API 重试队列、失败标记和重新检测清空返回首页；没有真实系统联网状态监听、真实请求失败捕获或真实队列重放。
- 消息中心已做本地版，订单类消息可跳转订单详情，订单和非订单消息都支持本地持久化已读状态和首页未读数联动；真实推送、跨设备已读同步和后端消息流仍未接入。
- 客服帮助已做本地版，支持客服热线系统拨号、投诉建议本地工单提交、受理/处理完成状态流转、处理记录展示和 AsyncStorage 冷启动恢复，但没有真实在线聊天、后端工单同步或真实客服处理记录。
- 个人中心已做本地版，顶部展示头像占位、昵称、脱敏手机号、实名/企业认证状态和资料同步状态；常用地址支持本地新增/编辑/删除前确认、联系人电话校验和 20 个上限拦截，常用联系人支持本地新增/编辑/删除、手机号格式校验和 50 个上限拦截，并可在发单页联系人建议中复用；实名认证支持本地提交、身份证正反面凭证占位、人脸核验占位、审核中展示、本地模拟驳回原因和重新提交；企业认证支持本地提交、营业执照凭证占位、审核中展示、本地模拟驳回原因和重新提交；消费记录支持完成、进行中、退款和时间本地筛选，也展示总消费统计、支付状态、托管金额、司机收入、退款进度，以及带券本地订单的原价、优惠券、优惠金额和实付金额；发票管理支持可开票订单多选、金额汇总、发票类型、企业抬头读取本地企业认证名称、个人抬头读取本地昵称、接收邮箱本地申请记录、增值税专用发票企业认证前置拦截以及审核通过、下载凭证、驳回原因和重新提交状态流转；优惠券支持本地查看、筛选、使用状态流转、发单固定价订单本地计价预览、发布后本地使用状态联动和消费记录明细展示；设置支持昵称、头像凭证、手机号换绑本地校验、登录密码本地修改、手机号保护/异地登录保护/订单通知/促销通知本地开关、本机设备检查占位、协议摘要、权限用途说明、权限状态本地检查和拒绝引导、版本更新本地检查和退出登录；我的评价支持本地查看、展示本地已提交并持久化在订单状态中的评价、按评分筛选并展示本地 mock 司机回复；个人中心本地变更会进入待同步状态，展示本地同步队列，支持本地标记同步失败和重试清空队列，但没有真实上传、真实提交、真实通知/隐私能力、真实账号安全风控或后端账号中心同步。
- 常用路线支持本地新增/编辑/删除/排序，复用发单已支持本地预填，并已接入 AsyncStorage 设备级本地持久化；常用路线变更会进入待同步状态，展示本地同步队列，支持本地标记同步失败和重试清空队列，但真实后端同步仍未接入。

### 3.4 认证流程

当前登录/注册仍只是本地门禁，不是真正认证；本地验证码固定码匹配、错误码拦截、会话恢复、过期、刷新和 AsyncStorage 设备级本地持久化已经补上，但仍不是短信服务、后端 token/refresh token 鉴权或服务端验证码校验。实名认证和企业认证都已支持本地提交、审核中、本地模拟驳回原因展示和重新提交；但两者都不包含真实证照上传、第三方人脸识别 SDK、后端审核或真实审核结果同步。

缺口：

- 没有后端 token 或 refresh token。
- 没有真实后端登录态；当前只是 AsyncStorage 本地会话快照，过期时间和刷新逻辑不能替代服务端鉴权。
- 没有短信接口、服务端验证码校验或验证码风控；当前只是本地固定演示码。
- 没有真实实名认证后端审核、真实证照上传、第三方人脸识别 SDK、真实审核结果同步和真实失败原因同步；当前失败原因只是本地模拟展示。
- 没有真实企业认证后端审核、真实证照上传、真实审核结果同步、真实失败原因同步和企业认证通过后的真实发票联动；当前失败原因和专票前置只是本地模拟展示。
- 没有退出后清理真实登录态。

## 4. 完全没开发的主要模块

### 4.1 货主端

- 实名认证真实审核、真实证照上传、第三方人脸识别 SDK、真实审核结果和真实失败原因同步。
- 企业认证真实审核、真实证照上传、真实审核结果和真实失败原因同步。
- 常用地址后端同步。
- 常用联系人后端同步。
- 真实支付与结算。
- 发票真实申请、真实文件下载、税务状态同步和企业认证通过后的真实联动。
- 优惠券真实发放、真实核销、后端同步、锁券和支付抵扣计价联动。
- 消费记录已有本地退款中记录、状态/时间筛选、总消费统计和支付/托管/退款明细展示，但没有真实支付流水、真实退款进度或后端筛选。
- 评价记录后端同步、真实图片上传和真实司机回复同步。
- 消息中心真实通知、跨设备已读同步、后端消息流和更多消息类型联动。
- 真实客服在线聊天、后端工单同步和真实客服处理记录。
- 设置页真实通知、真实隐私协议版本留痕、真实账号安全风控、多端设备列表、强制下线和后端同步能力；隐私政策本地确认、异地登录保护本地开关和本机设备检查占位已补。
- 网络异常页本地演练已补，本地 API 重试队列和失败标记已补；仍缺真实系统联网状态监听、真实请求失败捕获、真实队列重放和离线恢复策略。
- 真实系统权限申请弹窗和真实授权状态检测；本地权限用途说明、状态检查演练和拒绝引导已在设置页补齐。
- 引导页本地首启版已补，仍缺真实多版本运营引导、远程配置和活动投放策略。
- 真实应用商店更新、热更新通道、强制升级策略和更新包下载；当前仅有设置页本地版本检查。
- 埋点 SDK 接入。

### 4.2 司机端

司机端基本未开发，包括：

- 司机登录/注册。
- 实名认证。
- 车辆认证。
- 资质认证。
- 订单大厅。
- 抢单/竞价。
- 订单执行流程。
- 收入统计。
- 提现。
- 接单设置。
- 司机个人中心。

### 4.3 公共能力

- 地图定位。
- 地址搜索。
- 路线规划。
- 实时位置共享。
- 企业认证、发票等更多业务电话拨号入口。
- 即时聊天。
- 推送通知。
- 图片上传。
- 微信支付。
- 支付宝支付。
- 资金托管。
- 双向评价。
- 信用分。
- 黑名单。

### 4.4 后端与后台

- 没有后端服务代码。
- 没有数据库模型。
- 没有真实 API 调用层。
- 没有真实 API 鉴权刷新逻辑；本地认证会话已支持内存级刷新。
- 没有后台管理系统。

## 5. 当前代码风险

### 5.1 局部文件仍偏大

`App.tsx` 已从 3000 多行拆到约 100 多行，只承担导航壳职责。`HomeScreen.tsx` 已把个人中心拆到独立页面文件。当前仍偏大的文件主要是：

- `src/screens/ProfileCenterScreen.tsx`：个人中心约 750 行，地址、联系人、评价、消费、发票、优惠券、设置记录、实名和企业认证表单已拆出；资料同步状态已补本地队列、失败标记和重试清空，但后续仍应继续拆个人中心概览壳和资料同步状态卡。
- `src/screens/OrderDetailScreen.tsx`：详情页主流程约 954 行，司机/报价/位置/赏金/异常/取消/修改/评价组件已拆出，订单同步状态已补本地队列、失败标记和重试清空；后续还可继续拆同步状态卡和记录展示区块。
- `src/screens/OrderDraftScreen.tsx`：发单页约 750 行，货物、地址、车辆时间、增值服务、价格支付区块已拆出；货物图片凭证本地清单和草稿同步状态已补本地队列、失败标记和重试清空，但后续仍应继续拆确认发布摘要、草稿同步状态卡和底部发布操作。
- `src/styles.ts`：样式集中管理，短期方便，长期可按 screen/component 拆分。

下一轮不要再往这些文件里硬塞大功能，先按业务区块拆局部组件。要不然刚拆完的房子又拿水泥糊上，整挺热闹但没啥用。

### 5.2 数据模型仍偏轻

当前 `RecentOrder` 已支持首页、列表、详情、发布订单、状态流转、司机报价、本地位置跟踪、异常和评价的本地演示，但仍不够支撑真实后端、支付、真实位置轨迹、发票、退款、评价记录和客服工单。

### 5.3 页面状态仍是临时状态

顶层用字符串切屏，短期能跑，长期需要正式导航方案，例如 React Navigation。

### 5.4 测试覆盖偏少

已有测试覆盖了本地首启引导、引导跳转和完成状态持久化、网络异常页本地入口、本地 API 重试队列、失败标记和重新检测清空后返回首页、本地登录、注册协议、注册密码强度、未获取验证码禁止登录/注册、错误验证码拦截、验证码 60 秒重新获取冷却、同手机号 1 小时 5 次发送上限、验证码过期、退出登录、首页、发单、发单确认预览、议价订单发布和等待报价空状态、发单一口价范围、发单支付方式选择、本地优惠券计价预览、订单详情展示和发布后本地使用状态联动、发单期望送达时间偏好和格式校验、发单车辆细项选择、发单重量有效性、发单体积范围校验、发单数量有效性、发单货物描述 200 字上限、发单装货/卸货备注记录和 50 字上限、发单装货时间明确性、发单装货时间 2 小时后/7 天内/半小时刻度校验、发单装卸地址相同拦截、发单常用地址建议回填、个人中心新增本地地址在发单建议中复用、个人中心新增本地联系人在发单联系人建议中复用、发单货物图片凭证、本地凭证清单、移除最新凭证和 6 张上限、发单增值服务、装卸工人数和保价货值本地校验/展示、草稿自动恢复、草稿跨 App 重建恢复、草稿 24 小时有效期、草稿存储版本元数据、无效/过期快照清理、草稿同步边界和本地重试、订单列表、状态卡筛选、时间筛选、自定义日期范围筛选、搜索、详情进入、详情按来源返回订单列表或消息中心、待接单修改订单、待接单追加赏金、非待接单修改申请费用/退款/司机通知和审核流转、司机报价选择、订单联系人/司机/客服热线系统拨号、状态流转、带原因取消、已接单取消结算审核、确认送达、常用路线复用、常用路线本地新增/编辑/删除/排序、常用路线同步边界和本地重试、重新下单、重新下单复制来源和历史订单缺字段自动补齐、异常上报、异常类型必选、异常说明最小长度、异常图片凭证、异常处理状态流转、评价司机、评价内容最小长度和 200 字上限、评价提交后不可修改、匿名评价、评价图片凭证、评价记录进入个人中心、个人中心顶部身份摘要、我的评价筛选和司机回复展示、客服工单提交/持久化/冷启动恢复/本地处理记录流转、个人中心地址/联系人增删改、常用地址删除前确认、常用地址联系人电话校验和 20 个上限拦截、常用联系人手机号校验和 50 个上限拦截、实名认证本地提交、驳回原因和重新提交、企业认证本地提交、驳回原因和重新提交、消费筛选、消费退款筛选、消费时间筛选和支付生命周期明细、发票可开票订单多选、发票类型/抬头/邮箱申请、企业发票抬头读取本地企业认证名称、增值税专用发票企业认证前置拦截、发票审核/下载凭证、发票驳回原因和重新提交、优惠券筛选和本地使用状态、设置开关、账号资料、登录密码本地更新、账号安全异地登录保护本地开关和本机设备检查占位、个人中心资料同步队列和同步失败本地重试、设置页权限说明、权限状态本地检查和拒绝引导、隐私政策本地确认、版本更新本地检查、旧版设置快照补齐新版设置项和设置页退出登录，但仍缺少：

本轮新增覆盖：固定价订单选择本地可用优惠券并发布后，个人中心消费记录会展示原价、优惠券、优惠金额和实付金额。

- 登录/注册更多边界组合；本地验证码固定码匹配和错误验证码拦截已补。
- 发单草稿后端/跨设备同步；本地 AsyncStorage 适配层、版本校验、24 小时有效期、无效/过期快照清理和同步边界已补。

## 6. 建议补全顺序

### 阶段 1：补齐货主端本地 MVP 闭环

目标：不接后端，先让货主端“能完整走一单”。

已补：

- 完整本地发单表单。
- 议价订单本地发布和等待报价空状态。
- 发单支付方式本地选择，并在确认页和订单详情展示。
- 固定价订单本地可用优惠券选择、优惠金额/实付金额预览、订单详情展示和发布后个人中心本地已使用状态联动。
- 发单车辆细项本地选择，包括车长、尾板和篷布，并在确认页、订单卡片、订单列表搜索和详情展示。
- 货物图片凭证 1-6 张本地计数占位、本地清单和移除最新凭证。
- 表单本地校验。
- 独立确认发布页。
- 增值服务本地选择、装卸人数/保价货值细项记录和详情展示。
- 一口价本地范围校验。
- 货物重量 0.1 到 50 吨本地范围校验。
- 货物体积本地范围校验。
- 货物数量有效性校验。
- 货物描述 200 字上限校验。
- 装货/卸货备注本地记录和 50 字上限校验。
- 装货时间明确性、2 小时后、7 天内和半小时刻度校验。
- 期望送达时间偏好本地记录和格式校验。
- 装卸地址相同拦截。
- 发单页读取个人中心本地常用地址建议，支持回填装卸地址、联系人和手机号，并可复用新增/编辑后的本地地址。
- 发单页读取个人中心本地常用联系人建议，支持回填装卸联系人和手机号，并可复用新增/编辑后的本地联系人。
- 发布后把订单写入本地状态。
- 订单列表展示新订单。
- 订单详情展示主要信息。
- 待接单订单支持填写取消原因后取消和选择司机。
- 已接单订单支持系统拨号联系司机。
- 订单详情支持系统拨号联系装货联系人和卸货联系人。
- 待接单订单支持本地追加赏金并在详情展示曝光赏金。
- 待确认订单支持确认送达。
- 首页状态卡进入订单列表筛选。
- 首页订单状态标题右侧“全部订单”进入订单列表。
- 订单列表搜索。
- 订单列表本地时间筛选和自定义日期范围筛选。
- 详情按来源返回订单列表或消息中心。
- 非待接单修改申请、费用影响、退款状态、司机通知和本地确认/驳回记录。
- 常用路线复用发单。
- 常用路线本地新增、编辑、删除和排序。
- 常用路线本地后端同步边界，包含待同步、已同步、同步失败状态展示、管理面板本地同步队列、失败标记和重试清空入口。
- 已完成/已取消订单重新下单，并在历史订单缺字段时补齐可发布默认值，在新发布订单里记录复制来源。
- 未完成草稿本地存储适配层恢复，并覆盖 App 重建后的草稿恢复、同步状态展示和本地重试。
- 待接单订单修改并覆盖原订单。
- 订单本地后端同步边界，包含待同步、已同步、同步失败状态展示、详情页本地同步队列、失败标记和重试清空入口。
- 个人中心本地资料后端同步边界，包含待同步、已同步、同步失败状态展示、总览页本地同步队列、失败标记和重试清空入口。
- 异常上报本地表单、类型必选校验、说明最小长度校验、图片凭证占位和处理状态流转。
- 评价司机本地表单、标签必选校验、内容 6-200 字校验、提交后不可再次编辑、匿名开关、图片凭证占位和我的评价司机回复展示。
- 消息中心本地版、订单消息跳转和本地已读状态联动。
- 客服帮助本地版、客服热线系统拨号、投诉建议本地工单提交、本地持久化恢复和本地处理记录流转。
- 个人中心本地版。
- 个人中心顶部头像占位、脱敏手机号和实名/企业认证状态展示。
- 个人中心下钻页本地版。
- 常用地址本地新增、编辑、删除前确认、联系人电话校验和 20 个上限拦截。
- 常用联系人本地新增、编辑、删除、手机号格式校验、50 个上限拦截和发单页联系人建议复用。
- 实名认证本地提交、身份证正反面凭证占位、人脸核验占位、审核中展示、本地模拟驳回原因和重新提交。
- 企业认证本地提交、营业执照凭证占位、审核中展示、本地模拟驳回原因和重新提交。
- 消费记录退款状态、状态/时间筛选、总消费统计、支付生命周期明细和带券订单原价/优惠/实付展示。
- 可开票订单多选、金额汇总、发票类型、企业抬头读取本地企业认证名称、个人抬头读取本地昵称、接收邮箱本地申请记录、增值税专用发票企业认证前置拦截，以及审核通过、驳回原因、重新提交和下载凭证状态流转。
- 优惠券本地查看、筛选、使用状态流转、发单固定价本地计价预览、发布后本地状态联动和消费记录明细展示。
- 退出登录行为测试。
- 关键行为 Jest 测试。
- 本地认证会话保存、App 重建恢复、过期、刷新和退出清理。
- 本地首启引导、跳过/完成后进入认证页和完成状态持久化。
- 网络异常页本地演练、缓存可用范围提示、本地 API 重试队列、失败标记和重新检测清空返回首页。

仍要补：

- 认证会话接入真实后端 token 和 refresh token 刷新逻辑；本地 AsyncStorage TTL/刷新适配层已补。
- 草稿接入后端/跨设备同步；本地 AsyncStorage 持久化和同步边界已补。
- 常用路线真实后端同步、服务端冲突处理和跨设备同步；本地 AsyncStorage 持久化、同步队列和失败标记已补。
- 评价记录后端同步和真实司机回复联动；本地订单状态持久化已补。
- 实名认证真实后端审核、真实证照上传、第三方人脸识别 SDK、真实审核结果同步和真实失败原因同步。
- 企业认证真实后端审核、真实证照上传、真实审核结果同步和真实失败原因同步。
- 优惠券真实发放、真实核销、锁券、支付抵扣、真实后端计价联动和后端同步。
- 异常真实照片上传、异常链路里的真实客服工单和真实客服处理记录。

### 阶段 2：拆分代码结构

目标：避免 `App.tsx` 继续膨胀。

已补：

- 抽出类型定义。
- 抽出 mock 数据。
- 抽出通用组件。
- 拆出认证、首页、发单、订单列表、订单详情页面。
- 拆出个人中心页面，`HomeScreen.tsx` 只保留首页、消息和客服帮助入口。
- 拆出订单详情内的司机信息卡、司机报价卡、位置跟踪、追加赏金、异常上报、取消订单、修改申请和评价司机表单。
- 拆出发单页内的货物、装卸地址、车辆与时间、增值服务、价格和支付方式表单区块。
- 拆出个人中心内的常用地址、常用联系人、我的评价、消费记录、发票管理、优惠券和设置记录区块，并抽出财务记录共用 helper。
- 拆出个人中心内的实名认证和企业认证表单，父页面继续保留入口协调、概览和本地状态持久化。
- 保持现有测试通过。

仍要补：

- 继续拆分 `OrderDraftScreen.tsx` 内的确认发布摘要、草稿同步状态卡和底部发布操作区块。
- 继续拆分 `ProfileCenterScreen.tsx` 内的个人中心概览壳和资料同步状态卡。
- 按页面或组件逐步拆分 `src/styles.ts`，避免长期集中堆样式。

### 阶段 3：补货主端常用功能

目标：让货主端接近可演示版本。

已补：

- 常用地址本地查看、新增、编辑、删除前确认、联系人电话校验和 20 个上限拦截。
- 常用联系人本地查看、新增、编辑、删除、手机号格式校验、50 个上限拦截和发单页联系人建议复用。
- 常用路线本地新增、编辑、删除、排序、同步队列和同步失败本地重试。
- 我的评价记录本地查看，展示本地已提交并持久化在订单状态中的评价、已评价订单查看评价且不可再次编辑、本地 mock 司机回复，并支持评分筛选。
- 实名认证本地提交、身份证正反面凭证占位、人脸核验占位、审核中展示、本地模拟驳回原因和重新提交。
- 企业认证本地提交、营业执照凭证占位、审核中展示、本地模拟驳回原因和重新提交。
- 消费记录本地查看、完成/进行中/退款状态筛选、时间筛选、总消费统计和支付生命周期明细。
- 发票管理本地查看、可开票订单多选、金额汇总、发票类型/抬头/接收邮箱申请记录、企业抬头读取本地企业认证名称、增值税专用发票企业认证前置拦截、审核通过、驳回原因、重新提交和下载凭证状态流转。
- 优惠券本地查看、筛选、使用状态流转、发单固定价本地计价预览和发布后本地状态联动。
- 设置页本地查看、昵称/头像/手机号换绑/登录密码本地更新、促销通知开关、异地登录保护本地开关、本机设备检查占位、协议摘要、隐私政策本地确认、权限用途说明、权限状态本地检查和拒绝引导、版本更新本地检查和退出登录。
- 引导页本地首启版，支持跳过/完成后进入认证页并持久化完成状态。
- 网络异常页本地演练版，支持从首页进入、提示本地缓存可用范围，展示本地 API 重试队列，支持本地标记重试失败并重新检测清空后返回首页。

仍要补：

- 常用地址后端同步。
- 常用联系人后端同步。
- 常用路线真实后端同步、服务端冲突处理和跨设备同步。
- 我的评价真实图片上传、后端同步和真实司机回复同步。
- 实名认证真实证照上传、第三方人脸识别 SDK、真实审核结果同步和真实失败原因同步。
- 企业认证真实证照上传、真实审核结果同步和真实失败原因同步。
- 消费记录真实支付流水、真实退款进度和后端筛选。
- 发票真实下载、税务审核状态同步和企业认证通过后的真实联动。
- 优惠券真实发放、真实核销、后端同步、锁券和支付抵扣计价联动。
- 设置页真实密码修改、密码加密存储、短信换绑、真实通知推送、真实隐私协议版本留痕、真实账号安全风控、多端设备列表、强制下线、真实版本发布/更新包检测、真实系统权限申请/授权状态检测和后端同步。
- 真实多版本运营引导、远程配置和活动投放策略。
- 真实系统联网状态监听、真实请求失败捕获、真实队列重放和离线恢复策略。

### 阶段 4：司机端 MVP

目标：能演示司机接单和订单执行。

要补：

- 司机认证入口。
- 订单大厅。
- 订单详情。
- 接单操作。
- 装货/运输/送达状态流转。
- 收入概览。

### 阶段 5：后端/API/地图/支付

目标：从本地原型升级为真实业务系统。

要补：

- API client。
- 登录 token。
- 后端服务。
- 数据库。
- 地图 SDK。
- 文件上传。
- 支付 SDK。
- 推送。
- IM。

## 7. 下一步推荐执行项

阶段 2 的主结构拆分已经推进到个人中心独立页面，消息中心、客服帮助、个人中心和个人中心下钻页本地版也已补齐。下一步优先做“局部组件拆分 + 本地状态增强”。别现在就扑后端、地图、支付，那个范围太大，容易把项目干成半截烟囱。

下一步最小可交付：

1. `OrderDetailScreen.tsx` 的司机卡、报价列表、位置跟踪、赏金、异常、取消、修改和评价表单已拆；后续可继续拆记录展示区块。
2. `OrderDraftScreen.tsx` 的货物、地址、车辆时间、增值服务、价格支付区块已拆；草稿同步队列和失败标记已补本地演练，后续可继续拆确认发布摘要、草稿同步状态卡和发布操作。
3. `ProfileCenterScreen.tsx` 的地址、联系人、评价、消费、发票、优惠券、设置记录、实名和企业认证表单已拆；后续可继续拆概览壳和资料同步状态卡。
4. 订单状态、草稿、常用路线和个人中心资料的后端同步边界与本地重试入口已补；订单、草稿、常用路线和个人中心资料同步队列、失败标记已补本地演练，后续还要补真实 API、跨设备同步与冲突处理。
5. 修改订单已补本地费用影响、退款状态、司机通知和确认/驳回；取消订单已补本地违约提示、退款状态、客服审核和司机通知，真实后端计算仍待接入。
6. 把个人中心下钻页从本地增删改继续推进到真实账号中心同步、冲突提示和后端状态回放；失败队列目前只是本地演练，别当真后端。
7. 保持 `npm test`、`npx tsc --noEmit`、`npm run lint` 通过。
